import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.guilherme.gkperformancehub',
  appName: 'GKPerformanceHub',
  webDir: 'dist'
};

export default config;
